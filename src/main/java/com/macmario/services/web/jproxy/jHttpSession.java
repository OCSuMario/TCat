package com.macmario.services.web.jproxy;import com.macmario.io.thread.RunnableT;import java.net.Socket;import java.net.InetAddress;import java.net.UnknownHostException;import java.io.BufferedOutputStream;import java.io.BufferedInputStream;import java.io.File;import java.io.FileInputStream;/** * One HTTP connection *  * @file jHttpSession.java */public class jHttpSession extends jHttpProxyRunnableT {	public static final int SC_OK = 0;	public static final int SC_CONNECTING_TO_HOST = 1;	public static final int SC_HOST_NOT_FOUND = 2;	public static final int SC_URL_BLOCKED = 3;	public static final int SC_CLIENT_ERROR = 4;	public static final int SC_INTERNAL_SERVER_ERROR = 5;	public static final int SC_NOT_SUPPORTED = 6;	public static final int SC_REMOTE_DEBUG_MODE = 7;	public static final int SC_CONNECTION_CLOSED = 8;	public static final int SC_HTTP_OPTIONS_THIS = 9;	public static final int SC_FILE_REQUEST = 10;	public static final int SC_MOVED_PERMANENTLY = 11;	public static final int SC_CONFIG_RQ = 12;	private static jHttpProxyServer server;	public InetAddress serveraddress;		/** downstream connections */	private Socket client;	private BufferedOutputStream out;	private jHttpProxyClientInputStream in;	/** upstream connections */	private Socket HTTP_Socket;	private BufferedOutputStream HTTP_out;	private jHttpProxyServerInputStream HTTP_in;       	public jHttpSession(jHttpProxyServer server, Socket client) {                		try {			in = new jHttpProxyClientInputStream(server, this,					client.getInputStream());// ,true);			out = new BufferedOutputStream(client.getOutputStream());			jHttpSession.server = server;			this.client = client;		} catch (java.io.IOException io) {			try { client.close();} catch (java.io.IOException io2) {}			                        log(1,"Error create IO Streams , reason:"+io.getMessage());			return;		}				try {						serveraddress = InetAddress.getLocalHost();		} catch (UnknownHostException ue) {			log(1,"can't get own server address - "+ue.getMessage());			return;		}				start();	}	public Socket getLocalSocket() {  return client; }	public Socket getRemoteSocket() { return HTTP_Socket; 	}	public boolean isTunnel() { return in.isTunnel(); 	}	public boolean notConnected() { return HTTP_Socket == null; }	public void sendHeader(int a, boolean b) throws java.io.IOException {		sendHeader(a);		sendEndHeader();		out.flush();	}	public void sendHeader(int status, String content_type, long content_length) throws java.io.IOException {		sendHeader(status);		sendRowLine("Content-Length", String.valueOf(content_length));		sendRowLine("Content-Type", content_type);	}	public void sendLine(String s) throws java.io.IOException {		write(out, s + "\r\n");	}	public void sendRowLine(String header, String s) throws java.io.IOException {		write(out, header + ": " + s + "\r\n");	}	public void sendEndHeader() throws java.io.IOException {		write(out, "\r\n");	}        int id=0;                @Override	public void run() {                setRunning();                server.increaseNumConnections();                id=server.getConnextionCount();		log(1, "handle[ "+id+" ] new connection/session")   ;                		try {			handleRequest();		} catch (java.io.IOException io) {			log(1,"run: handleRequest [ "+id+" ] with exception " + io.getMessage() );                }		try {			// close downstream connections			in.close(); 			out.close();			client.close();			// close upstream connections (webserver or other proxy)			if (!notConnected()) {				HTTP_Socket.close();				HTTP_out.close();				HTTP_in.close();			}		} catch (java.io.IOException io) {			log(1,"run: [ "+id+" ] close connection" + io.getMessage());		}		server.decreaseNumConnections();				log(1,"finish [ "+id+" ] http session");                                setClosed();	} /*End run() */    public void sendErrorMSG(int a, String info) throws java.io.IOException  {		String statuscode = sendHeader(a);		String localhost = "localhost";		try {			localhost = InetAddress.getLocalHost().getHostName() + ":"					+ server.port;		} catch (UnknownHostException e_unknown_host) {		}		String msg = "<!DOCTYPE HTML PUBLIC \"-//W3C//DTD HTML 4.0 Transitional//EN\"><html>\r"				+ "<!-- "+version.SERVERNAME+" error message --><HEAD>\r" + "<TITLE>"				+ statuscode				+ "</TITLE>\r"//				+ "<link rel=\"stylesheet\" type=\"text/css\" href=\"http://"//				+ localhost//				+ "/style.css\"></HEAD>\r" // use css style sheet in htdocs				+ "<BODY BGCOLOR=\"#FFFFFF\" TEXT=\"#000000\" LINK=\"#000080\" VLINK=\"#000080\" ALINK=\"#000080\">\r"				+ "<h2 class=\"headline\">HTTP "				+ statuscode				+ " </h2>\r"				+ "<HR size=\"4\">\r"				+ "<p class=\"i30\">Your request for the following URL failed:</p>"				+ "<p class=\"tiagtext\"><a href=\""				+ in.getFullUrl()				+ "\">"				+ in.getFullUrl()				+ "</A> </p>\r"				+ "<P class=\"i25\">Reason: "				+ info				+ "</P>"				+ "<HR size=\"4\">\r"				+ "<p class=\"i25\"><A HREF=\"http://jhttp2.sourceforge.net/\">jHTTPp2</A> HTTP Proxy, Version "				+ version.getServerVersion()				+ " at "				+ localhost				+ "<br>Copyright &copy; 2025 "+com.macmario.general.Version.prodauthor+"</p>\r"				+ "<p class=\"i25\"><A HREF=\"http://"				+ localhost				+ "/\">"+version.SERVERNAME+" local website</A> <A HREF=\"http://"				+ localhost				+ "/"				+ server.WEB_CONFIG_FILE				+ "\">Configuration</A></p>"				+ "</BODY></HTML>";		sendRowLine("Content-Length", String.valueOf(msg.length()));		sendRowLine("Content-Type", "text/html; charset=iso-8859-1");		sendEndHeader();		write(out, msg);		out.flush();	}	public String sendHeader(int a) throws java.io.IOException {		String stat;		switch (a) {		case 200:			stat = "200 OK";			break;		case 202:			stat = "202 Accepted";			break;		case 300:			stat = "300 Ambiguous";			break;		case 301:			stat = "301 Moved Permanently";			break;		case 400:			stat = "400 Bad Request";			break;		case 401:			stat = "401 Denied";			break;		case 403:			stat = "403 Forbidden";			break;		case 404:			stat = "404 Not Found";			break;		case 405:			stat = "405 Bad Method";			break;		case 413:			stat = "413 Request Entity Too Large";			break;		case 415:			stat = "415 Unsupported Media";			break;		case 501:			stat = "501 Not Implemented";			break;		case 502:			stat = "502 Bad Gateway";			break;		case 504:			stat = "504 Gateway Timeout";			break;		case 505:			stat = "505 HTTP Version Not Supported";			break;		default:			stat = "500 Internal Server Error";		}		sendLine(server.getHttpVersion() + " " + stat);		sendRowLine("Server", version.getServerIdentification());		if (a == 501)			sendRowLine("Allow", "GET, HEAD, POST, PUT, DELETE, CONNECT");		sendRowLine("Cache-Control", "no-cache, must-revalidate");		sendRowLine("Connection", "close");		return stat;	}	/** the main routine, where it all happens */	public void handleRequest() throws java.io.IOException {		InetAddress remote_host;		jHttpProxyRead remote_in = null;		int remote_port;		byte[] b = new byte[65536];		int numread = in.read(b);                log(1,"handleRequest [ "+id+" ] start");		while (true) { // with this loop we support persistent connections			if (numread == -1) { // -1 signals an error                                int state = in.getStatusCode();                                log(1,"handleRequest [ "+id+" ] read "+numread+" bytes - status:"+state);				if (state != SC_CONNECTING_TO_HOST) {					switch (state) {					case SC_CONNECTION_CLOSED:						break;					case SC_CLIENT_ERROR:						sendErrorMSG(400,								"Your client sent a request that this proxy could not understand. ("										+ in.getErrorDescription() + ")");						break;					case SC_HOST_NOT_FOUND:						sendErrorMSG(								504,								"Host not found.<BR>"+version.SERVERNAME+" was unable to resolve the hostname of this request. <BR>Perhaps the hostname was misspelled, the server is down or you have no connection to the internet.");						break;					case SC_INTERNAL_SERVER_ERROR:						sendErrorMSG(500,								"Server Error! (" + in.getErrorDescription()										+ ")");						break;					case SC_NOT_SUPPORTED:						sendErrorMSG(501,								"Your client used a HTTP method that this proxy doesn't support: ("										+ in.getErrorDescription() + ")");						break;					case SC_URL_BLOCKED:						sendErrorMSG(								403,								(in.getErrorDescription() != null										&& in.getErrorDescription().length() > 0 ? in										.getErrorDescription()										: "The request for this URL was denied by the "+version.SERVERNAME+" URL-Filter."));						break;					// case SC_REMOTE_DEBUG_MODE: remoteDebug(); break;					case SC_HTTP_OPTIONS_THIS:						sendHeader(200);						sendEndHeader();						break;					case SC_FILE_REQUEST:						file_handler();						break;					case SC_CONFIG_RQ:						admin_handler(b);						break;					// case SC_HTTP_TRACE:					case SC_MOVED_PERMANENTLY:						sendHeader(301);						write(out, "Location: " + in.getErrorDescription()								+ "\r\n");						sendEndHeader();						out.flush();					default:					}					break; // return from main loop.				} else { // also an error because we are not connected (or to							// the wrong host)							// Creates a new connection to a remote host.					if (!notConnected()) {						try {							HTTP_Socket.close();						}						catch (java.io.IOException e_close_socket) {						}					}					numread = in.getHeaderLength(); // get the header length					if (!server.useProxy) {// sets up hostname and port						remote_host = in.getRemoteHost();						remote_port = in.remotePort;					} else {						remote_host = server.proxy;						remote_port = server.proxy_port;					}					// if (server.ldebug)server.writeLog("Connect: " +					// remote_host + ":" + remote_port);					try {						connect(remote_host, remote_port);					} catch (java.io.IOException e_connect) {						if (server.ldebug)							server.writeLog("handleRequest: " + e_connect.toString());						sendErrorMSG(								502,								"Error while creating a TCP connection to ["										+ remote_host.getHostName()										+ ":"										+ remote_port										+ "] <BR>The proxy server cannot connect to the given address or port ["										+ e_connect.toString() + "]");						break;					} catch (Exception e) {						server.writeLog("handleRequest: " + e.toString());						sendErrorMSG(500, "Error: " + e.toString());						break;					}					if (!in.isTunnel() || (in.isTunnel() && server.useProxy)) {						// no SSL-Tunnel or SSL-Tunnel with another remote						// proxy: simply forward the request						HTTP_out.write(b, 0, numread);						HTTP_out.flush();					} else {						// SSL-Tunnel with "CONNECT": creates a tunnel						// connection with the server						sendLine(server.getHttpVersion()								+ " 200 Connection established");						sendRowLine("Proxy-Agent", version.getServerIdentification());						sendEndHeader();						out.flush();					}					// read the data from the remote server					if (remote_in != null) {						remote_in.close(); // close Read thread					}					remote_in = new jHttpProxyRead(server, this, HTTP_in, out); 																									server.addBytesWritten(numread);				}			}			while (true) { // reads data from the client				numread = in.read(b);				// if (server.ldebug)server.writeLog("jHttpSession: " +				// numread + " Bytes read.");				if (numread != -1) {					HTTP_out.write(b, 0, numread);					HTTP_out.flush();					server.addBytesWritten(numread);				} else					break;			} // end of inner loop for data transfer		}// end of main loop		out.flush();		if (!notConnected() && remote_in != null)			remote_in.close(); // close Read thread                                log(1,"handleRequest [ "+id+" ] stop");                		return;	}	/** connects to the given host and port */	public void connect(InetAddress host, int port) throws java.io.IOException {                log(1, "connect - like to connect to "+((host==null)?"NULL":host)+":"+port);		HTTP_Socket = new Socket(host, port);		HTTP_in = new jHttpProxyServerInputStream(server, this,				HTTP_Socket.getInputStream(), false);		HTTP_out = new BufferedOutputStream(HTTP_Socket.getOutputStream());	}	/** converts a String into a Byte-Array to write it with the OutputStream */	public void write(BufferedOutputStream o, String p) throws java.io.IOException {		o.write(p.getBytes(), 0, p.length());	}	/**	 * Small webserver for local files in {app}/htdocs	 * 	 * @since 0.4.04	 */	public void file_handler() throws java.io.IOException {		if (!server.www_server) {			sendErrorMSG(500,					"The "+version.SERVERNAME+" built-in WWW server module is disabled.");			return;		}		String filename = in.url;		if (filename.equals("/"))			filename = "index.html"; // convert / to index.html		else if (filename.startsWith("/"))			filename = filename.substring(1);		if (filename.endsWith("/"))			filename += "index.html"; // add index.html, if ending with /		File file = new File("htdocs/" + filename); // access only files in													// "htdocs"		if (!file.exists() || !file.canRead() // be sure that we can read the												// file				|| filename.indexOf("..") != -1 // don't allow ".." !!!				|| file.isDirectory()) { // dont't read if it's a directory			sendErrorMSG(404, "The requested file /" + filename					+ " was not found or the path is invalid.");			return;		}		int pos = filename.lastIndexOf("."); // MIME type of the specified file		String content_type = "text/plain"; // all unknown content types will be											// marked as text/plain		if (pos != -1) {			String extension = filename.substring(pos + 1);			if (extension.equalsIgnoreCase("htm")					|| (extension.equalsIgnoreCase("html")))				content_type = "text/html; charset=iso-8859-1";			else if (extension.equalsIgnoreCase("jpg")					|| (extension.equalsIgnoreCase("jpeg")))				content_type = "image/jpeg";			else if (extension.equalsIgnoreCase("gif"))				content_type = "image/gif";			else if (extension.equalsIgnoreCase("png"))				content_type = "image/png";			else if (extension.equalsIgnoreCase("css"))				content_type = "text/css";			else if (extension.equalsIgnoreCase("pdf"))				content_type = "application/pdf";			else if (extension.equalsIgnoreCase("ps")					|| extension.equalsIgnoreCase("eps"))				content_type = "application/postscript";			else if (extension.equalsIgnoreCase("xml"))				content_type = "text/xml";		}		sendHeader(200, content_type, file.length());		sendEndHeader();		BufferedInputStream file_in = new BufferedInputStream(				new FileInputStream(file));		byte[] buffer = new byte[4096];		int a = file_in.read(buffer);		while (a != -1) { // read until EOF			out.write(buffer, 0, a);			a = file_in.read(buffer);		}		out.flush();		file_in.close(); // finished!	}	/**	 * @since 0.4.10b	 */	public int getStatus() {		return in.getStatusCode();	}	/**	 * @since 0.4.20a admin webpage	 */	public void admin_handler(byte[] b) throws java.io.IOException {		if (!server.webconfig) {			sendErrorMSG(500, "The web-based configuration module is disabled.");			return;		}		jHttpProxyAdmin admin = null;		String filename = in.url;		if (in.post_data_len > 0) { // if the client used "POST" then append the									// data to the filename			filename = filename					+ "?"					+ new String(b, in.getHeaderLength() - in.post_data_len,							in.post_data_len);		}                log(1,"admin_handler file ->"+filename+"<-");		if (filename.startsWith("/"))			filename = filename.substring(1);		String adminpage = "";		try {			admin = new jHttpProxyAdmin(filename, server);			admin.getWebAdmin();			adminpage = admin.HTMLAdmin();		} catch (Exception e) {			e.printStackTrace();			server.writeLog(version.SERVERNAME+"Admin Exception: " + e.getMessage());		}		int adminlen = adminpage.length();		if (adminlen < 1) {			sendErrorMSG(500, "Error Message from the Web-Admin modul: "					+ jHttpProxyAdmin.error_msg);		} else {			sendHeader(200, "text/html", adminlen);			sendEndHeader();			write(out, adminpage);		}		out.flush();	}}